import { UserProtection } from "../../../views/user/settings/Protected";

export function UserProtectionPage({ children }: { children: React.ReactNode }) {
  return<>
  <UserProtection children={children}/>
  </>
}
